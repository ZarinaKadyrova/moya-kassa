rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Все документы доступны для чтения и записи
    // (для семейного приложения без авторизации)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
